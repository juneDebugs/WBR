# Phase 4 Smoketest Run — 2026-06-28 (in-session second opinion)

Run of `docs/smoketests/phase-4-strip-login-imagery.md` against branch `phase-4-strip-login-imagery` prior to commit. Recorded as a pre-merge automation pass; the four-step smoketest itself remains the canonical specification, and the items deferred below are batched into the sprint's user-acceptance-testing (UAT) round once code work is largely complete.

## Environment for this run

- Local clone on branch `phase-4-strip-login-imagery`, off `main` at `b15be68`.
- Step 2 used local production build (`pnpm --filter <app> build && pnpm --filter <app> start`) — meetings on port 3002, sponsor on port 3003.
- Step 4 used Tier-C local production build with `git stash` baseline-capture pattern (same as the Phase 2 precedent). Tier B (Vercel preview) deferred until after push.

## Step 1 — Code-level inspection [contract]

Result: **PASS** (both files).

| File | `images.unsplash.com` count | `<img` inside JSX comment block | total `<img` count |
|---|---|---|---|
| `apps/meetings/app/login/page.tsx` | 3 | 1 | 1 |
| `apps/sponsor/app/login/page.tsx` | 3 | 1 | 1 |

All three contract sub-checks pass per the documented Pass criteria. The `slides` array URLs (three per file) are preserved; the `<img>` tag is present in source (count = 1) and lives inside the JSX comment block (`awk` range extraction count = 1); no active `<img>` element renders.

## Step 2 — Rendered HTML inspection [contract]

Result: **PASS** (both files). Measured against running local production-build servers.

| App | URL | Unsplash URL substring count in served HTML | Carousel headline text substring count |
|---|---|---|---|
| meetings | `http://localhost:3002/login` | 0 | 1 (`Connect, Meet`) |
| sponsor | `http://localhost:3003/login` | 0 | 1 (`Maximize Your`) |

No Unsplash URL leaks into the served HTML. The carousel headline text from the `slides` array is still present in served HTML on both apps, confirming the text-rotation block survived the strip and the `slides` array still drives the lg+ left-panel text rotation.

## Step 3 — Visual layout check [contract]

Result: **PROXY PASS** (deferred to UAT for real-browser viewport-render confirmation).

Per the user's pre-Phase-2 direction (carried forward as a standing pattern for the sprint): in-session run uses contract-tier observable proxies for the real-browser visual check. The hands-on Chrome + Safari mobile-viewport render verification batches into the sprint UAT round.

**In-session contract observables (all PASS):**

| Observable | Result |
|---|---|
| Lighthouse mobile-form-factor run completes without `viewport-insight` or `meta-viewport` regression | PASS on both apps |
| Served HTML contains the panel parent `<div className="hidden lg:flex lg:w-1/2 relative overflow-hidden">` | PASS on both apps (`grep` against curl output) |
| Served HTML contains both gradient overlay `<div>`s with `bg-gradient-to-b ...` and `bg-gradient-to-t ...` classes | PASS on both apps |
| Served HTML contains zero `<img` tags inside the lg+ left panel surface | PASS on both apps |
| `total-byte-weight` change between baseline and post matches the documented 428 KB imagery budget | PASS on both apps (see Step 4) |

**Deferred to UAT round (real-browser visual rendering):**

- **UAT-1.** Open meetings `/login` and sponsor `/login` on the Phase 4 Vercel preview URL in Chrome DevTools mobile profile (iPhone 14 Pro / 390×844). Verify: (a) login form renders with all elements visible (email + password + sign-in button + Google sign-in + demo accounts block); (b) `document.documentElement.scrollWidth <= window.innerWidth` in DevTools console; (c) no horizontal scroll bar at bottom of viewport.
- **UAT-2.** Open both `/login` pages in Safari Responsive Design Mode (iPhone 14 Pro / iPhone 15 profile). Same observables as UAT-1.
- **UAT-3.** Open both `/login` pages in a desktop browser at width ≥ 1024 px (lg+ Tailwind breakpoint). Verify: (a) left panel renders the gradient backdrop visibly; (b) carousel headline text + subtitle text visible in the bottom-left of the left panel; (c) carousel dots cycle through 3 dots over 5-second intervals; (d) DevTools Elements panel search for `img` inside `.hidden.lg\:flex` ancestor returns zero matches.
- **UAT-4.** Real iOS Safari (iPhone) + real Android Chrome (Pixel) at the Phase 4 Vercel preview URL: confirm the gradient backdrop renders cleanly, the carousel text cycles, the login form is interactive. Log any deviation as a Phase 13 perf-delta-report real-device follow-up.

## Step 4 — Lighthouse mobile total transfer [perf-bar tier C]

Result: **PASS** on both apps. Measured Tier-C local production build using the `git stash` baseline-capture pattern (same as the Phase 2 precedent). Tier B (Vercel preview) deferred until after push.

| Measurement | meetings `total-byte-weight` | sponsor `total-byte-weight` |
|---|---|---|
| Baseline (Phase 4 changes stashed; pre-Phase-4 state restored on disk; rebuilt) | 565,365 bytes (552 KB) | 563,571 bytes (550 KB) |
| Post-change (Phase 4 branch state; `<img>` block commented out) | 126,774 bytes (124 KB) | 124,971 bytes (122 KB) |
| Delta (baseline − post) | 438,591 bytes (-428 KB) | 438,600 bytes (-428 KB) |

**Meetings pass criterion:** `post.total_byte_weight_bytes ≤ 250 * 1024` (256,000 bytes). **Result:** 126,774 ≤ 256,000 ✓ PASS. (Headroom: ~127 KB below threshold.)

**Sponsor pass criterion:** `baseline - post ≥ 350 * 1024` (358,400 bytes). **Result:** 438,600 ≥ 358,400 ✓ PASS. (Delta exceeds threshold by ~80 KB; matches the documented 428 KB headline image budget exactly.)

The post-change Lighthouse `total-byte-weight` for both apps is essentially identical (~123 KB), reflecting the shared bundle + page weight after the imagery is stripped. The 428 KB baseline−post delta matches the three-image Unsplash budget documented in PRD §6 Phase 4 to within 1 byte of measurement noise.

**Sponsor baseline note:** the PRD §6 Phase 4 baseline of 1289 KB for sponsor `/login` comes from the 2026-06-18 perf recon measured against the production deployment with authenticated session cookies and the still-in-place `/api/attendees` preload at the root layout (Phase 3 target). This run's Tier-C local build measures `/login` unauthenticated (no session cookies, no API calls) — so the 550 KB local baseline reflects Phase 4 isolation only (page bundle + imagery, no preload). When Phase 3 ships and relocates the `/api/attendees` preload off the sponsor root layout, the post-Phase-3+Phase-4 production-deploy `total-byte-weight` should approach the ~120 KB Tier-C local figure, satisfying PRD AC #2 "toward ≤ 250 KB."

Deferred to UAT: Step 4 Tier-B run on the Phase 4 Vercel preview URL (both `wbr-meetings` and `wbr-sponsor` previews) once the PR is open. Run log expected to be appended here or in a follow-up run log file.

## Step summary (this run)

| Step | Category | Environment | Status |
|---|---|---|---|
| 1. Code-level inspection | contract | source files | PASS |
| 2. Rendered HTML inspection | contract | local prod build (Tier C) | PASS |
| 3. Visual layout check | contract (proxy) | local prod build + Lighthouse + curl | PROXY PASS; UAT-1 through UAT-4 deferred |
| 4. Lighthouse mobile total transfer | perf-bar tier C | local prod build with `git stash` baseline | PASS (meetings + sponsor) |

## UAT batch deferred items

- **UAT-1.** Chrome DevTools mobile viewport check on the Phase 4 Vercel preview for meetings `/login` and sponsor `/login` (see Step 3).
- **UAT-2.** Safari Responsive Design Mode check on both apps (see Step 3).
- **UAT-3.** Desktop ≥ 1024 px viewport visual check on both apps (gradient backdrop + carousel text + cycling dots, no `<img>` in DOM) (see Step 3).
- **UAT-4.** Real iOS Safari + real Android Chrome confirmation on both apps' Vercel preview (see Step 3).
- **UAT-5.** Step 4 Tier-B Vercel preview Lighthouse re-run on `wbr-meetings` and `wbr-sponsor` previews once the PR is open. Compare to this run's Tier-C local-prod-build numbers and the PRD §6 Phase 4 thresholds.

All UAT items are non-breaking and batched into the sprint UAT round per the `feedback_finding_protocol` refinement adopted in Phase 2 (peer-to-peer / real-device verification batches into UAT, not blocking per-phase commits).

## Net read

Phase 4 ships cleanly on the in-session second-opinion automation. The Unsplash imagery is no longer served to either `/login` route; the source `<img>` block + `slides` array remain in source for quick re-enablement (one comment-removal); Lighthouse confirms the 428 KB transfer reduction matches the documented headline budget exactly on both apps; both apps' POST `total-byte-weight` is well below the PRD §6 Phase 4 thresholds. Real-browser visual verification batches into UAT per the established sprint pattern.
