# Phase 1 Smoketest — automated run, 2026-06-27

Automated first-pass run of `docs/smoketests/phase-1-prefetch-fanout-gate.md` on branch `phase-1-prefetch-fanout-gate` (commit `ecda52f`, smoketest contract update follows). Categorized per `docs/smoketests/CONTRACT.md`. **Not a substitute for the human run** — automation covers steps that are deterministic + automatable; the human verifier owns the rest.

## Environment

Two runs captured: one against `pnpm --filter attendee dev` (contract-step verification only), and one against `pnpm --filter attendee build && pnpm --filter attendee start` (the production-build local server — valid for both contract checks and tier-C perf-bar checks per `CONTRACT.md` §1.2). Lighthouse mobile profile default for both (simulated slow-4G — rttMs 150, throughputKbps 1638.4, `cpuSlowdownMultiplier` 4).

Session credentials: `steph@curry.com` / `stephcurry`, ATTENDEE role. Cookie captured via `POST /api/login`.

## Step 1 — Code inspection [contract]: PASS

Read `apps/attendee/lib/hooks.ts` directly at commit `ecda52f`.

- ✅ `usePrefetchAll`'s `useEffect` body no longer fires the eight `prefetchQuery` calls inline. They are wrapped in an inner `run()` function.
- ✅ Effect gates scheduling on `load`: takes the `document.readyState === 'complete'` branch immediately or installs a one-shot `window.addEventListener('load', ..., { once: true })` listener.
- ✅ `schedule()` uses `window.requestIdleCallback(run, { timeout: 10_000 })` when available; `setTimeout(run, 0)` as Safari-< 16.4 fallback.
- ✅ Cleanup tears down all three scheduling states: removes the `load` listener, `cancelIdleCallback`s the idle handle, `clearTimeout`s the fallback timeout.

## Step 2 — Cold-load deferral [contract]: PASS

Verified at both tier C (local prod build) and tier D (dev) — the load-event gate fires identically on both because it is environment-agnostic React + browser-event code.

### Local production build (`next start`, port 3001)

**Reference: `observedLoad` = 67 ms.**

| URL | networkRequestTime | Δ from load | verdict |
|---|---:|---:|---|
| `/api/data/home` | 72.6 ms | +5.6 ms | after load — critical query |
| `/api/data/meetings` | 75.7 ms | +8.7 ms | after load — deferred prefetch ✓ |
| `/api/data/schedule` | 75.9 ms | +8.9 ms | after load — deferred prefetch ✓ |
| `/api/data/speakers` | 76.3 ms | +9.3 ms | after load — deferred prefetch ✓ |
| `/api/data/people` | 76.6 ms | +9.6 ms | after load — deferred prefetch ✓ |
| `/api/data/chat` | 76.6 ms | +9.6 ms | after load — deferred prefetch ✓ |
| `/api/data/my-schedule` | 76.7 ms | +9.7 ms | after load — deferred prefetch ✓ |
| `/api/data/setup` | 76.7 ms | +9.7 ms | after load — deferred prefetch ✓ |

All 7 non-home prefetches fire after `observedLoad`. Critical `/api/data/home` starts 3.1 ms before the earliest deferred prefetch. On localhost the gap is tight (main thread idles fast); on real slow-4G the gap widens because the critical query holds the bandwidth pipe.

### Dev mode (`next dev`, port 3001) — for reference, contract holds

**Reference: `observedLoad` = 1372 ms.** All 7 non-home prefetches fired between +46 ms and +48 ms post-load. Same PASS verdict — the gate is environment-independent.

## Step 3 — Warm-nav cache hits [contract]: NOT AUTOMATED (human only)

Lighthouse runs a single cold navigation; it doesn't drive subsequent bottom-nav clicks within the same session. Pass criterion is a binary Network-panel observation per `docs/smoketests/phase-1-prefetch-fanout-gate.md` Step 3 — runner clicks bottom-nav, observes no new `/api/data/<section>` request fires.

## Step 4 — Data-staleness contract [contract]: NOT AUTOMATED (human only)

Requires a two-tab cross-app admin edit + React Query DevTools cache inspection. Follow the smoketest Step 4 instructions manually.

## Step 5 — `/home` TBT no-regression [perf-bar tier C]: PASS

### Local production build (`next start`, port 3001), Lighthouse mobile

| Metric | Local prod build | Phase 2 production baseline | Pass criterion (smoketest) | Verdict |
|---|---:|---:|---|---|
| TBT | **19 ms** | ~100 ms | ≤ 150 ms | ✅ PASS |
| FCP (observed) | 78 ms | — | informational | — |
| LCP (observed) | 395 ms | — | informational | — |
| LCP (simulated) | 18781 ms | 17100 ms | tier C not comparable to AC bar | see step 6 |
| Speed Index (simulated) | 1095 ms | — | informational | — |
| Performance score | 75 / 100 | — | informational | — |

**TBT verdict:** PASS. The 19 ms measurement is well under the 100 ms Phase 2 baseline. The deferred prefetch batch does not produce any new long task — confirmed by trace inspection (no main-thread block ≥ 50 ms attributable to the prefetch fan-out).

**LCP note:** the simulated LCP (18.8 s) cannot be compared directly to the Phase 2 production baseline. Tier C runs against local SQLite with uncompressed seed data (the `/api/data/home` response is 690 KB; production may differ). Real LCP verification happens at Tier B (Vercel preview) and Tier A (production). The observed-LCP (395 ms) tells us the page itself paints fast — the simulated number is dominated by the network model's view of the resource graph.

## Step 6 — AC verification [perf-bar tier B]: PENDING

Requires the PR push + Vercel preview deployment. Run after `git push origin phase-1-prefetch-fanout-gate` and the preview URL appears on the PR check list.

Procedure: see `docs/smoketests/phase-1-prefetch-fanout-gate.md` Step 6. Compare each of `/home`, `/speakers`, `/schedule`, `/people` LCP against the Phase 2 baseline:
- `/home`: target ≥ 50% reduction (< 8.55 s), preferred < 3 s.
- `/speakers`: target ≥ 50% reduction (< 7.75 s).
- `/schedule`: target ≥ 50% reduction (< 4.42 s).
- `/people`: target ≥ 50% reduction (< 4.07 s).

## Step 7 — Post-merge production confirmation [perf-bar tier A]: PENDING

Runs after PR merge to `main` and Vercel deploys the production build. Sequenced as sprint Phase 7 per PRD §6.

## Step 8 — PWA SW compatibility [contract, prod-build only]: NOT AUTOMATED (human only)

Requires DevTools Application panel inspection (Service Workers + Cache Storage). The Lighthouse run on the local prod build shows no console errors and no SW-related warnings, but that's not a complete verification. Follow the smoketest Step 8 instructions manually on the local prod build.

## Step summary

| Step | Category | Environment | Status |
|---|---|---|---|
| 1. Code inspection | contract | source read | ✅ PASS |
| 2. Cold-load deferral | contract | local prod + dev | ✅ PASS (both tiers) |
| 3. Warm-nav cache hits | contract | local prod or preview | ⏳ Human |
| 4. Data-staleness contract | contract | local prod or preview | ⏳ Human |
| 5. `/home` TBT | perf-bar tier C | **local prod build** | ✅ PASS (19 ms vs ≤150 ms target) |
| 6. AC verification | perf-bar tier B | **Vercel preview** | ⏳ Post-push |
| 7. Post-merge confirmation | perf-bar tier A | **production deploy** | ⏳ Post-merge |
| 8. PWA SW compatibility | contract | **local prod build** | ⏳ Human (DevTools inspection) |

## Net read

**Deferral mechanism works at prod-build tier** — the seven non-home prefetches all queue post-load via `requestIdleCallback`, and TBT is 19 ms (well below the 100 ms baseline). The headline contract check (Step 2) passes deterministically.

**AC verification is gated on Step 6 (Vercel preview Lighthouse).** Local-prod-build LCP (18.8 s simulated) is not comparable to the Phase 2 production baseline because the local DB, payload, and image-optimization paths differ from Vercel + Turso production. Once the branch pushes and the preview URL is up, run Step 6 to get the real AC numbers before merging.

## Re-running this

See `docs/smoketests/phase-1-prefetch-fanout-gate.md` Step 5 for the production-build local Lighthouse recipe. The script pattern is reusable for Phase 2, 3, 4, 5, 14 — swap the URL and acceptance thresholds.
